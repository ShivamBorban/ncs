pipeline {
    agent any
    stages {
        stage('Build Docker Image') {
            steps {
                sh 'docker build -t image1 .'
            }
        }
        stage('Trigger Deploy') {
            steps {
                build job: 'Deploy App'
            }
        }
    }
}
